<UserControl x:Class="Test.MyZone"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Test"
             mc:Ignorable="d" 
             d:DesignHeight="160" d:DesignWidth="720">
    <!--<UserControl.DataContext>
        <DataTemplate DataType="local:PlayerData"/>
    </UserControl.DataContext>-->
    <Grid>
        <Grid Name="gridAll">
            <Grid.RowDefinitions>
                <RowDefinition Height="5*"/>
                <RowDefinition Height="150*"/>
                <RowDefinition Height="5*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="10*"/>
                <ColumnDefinition Width="150*"/>
                <ColumnDefinition Width="10*"/>
                <ColumnDefinition Width="450*"/>
                <ColumnDefinition Width="10*"/>
                <ColumnDefinition Width="80*"/>
                <ColumnDefinition Width="10*"/>
            </Grid.ColumnDefinitions>
            <local:RolesUC x:Name="rolesUC"  Grid.Column="1" Grid.Row="1" >
            </local:RolesUC>
            <local:BuildingsUC x:Name="buildingsUC" Grid.Column="3" Grid.Row="1"/>
        </Grid>
    </Grid>
</UserControl>
